name: PlanPing scraper

on:
  schedule:
    - cron: '0 3 * * *'   # open data scrape
    - cron: '0 4 * * *'   # idox nightly scrape — batch 1 of 4
    - cron: '50 4 * * *'  # idox nightly scrape — batch 2 of 4
    - cron: '40 5 * * *'  # idox nightly scrape — batch 3 of 4
    - cron: '30 6 * * *'  # idox nightly scrape — batch 4 of 4
    - cron: '20 7 * * *'  # arcus nightly scrape
    - cron: '35 7 * * *'  # civica nightly scrape (St Albans)
    - cron: '50 7 * * *'  # northgate nightly scrape (Runnymede)
    - cron: '55 7 * * *'  # northgate servlet-family nightly scrape (Hartlepool, High Peak, Staffordshire Moorlands)
    - cron: '0 8 * * *'   # northgate South Tyneside nightly scrape
    - cron: '5 8 * * *'   # NI Planning Portal nightly scrape
    - cron: '8 8 * * *'   # agileapplications.co.uk nightly scrape (Middlesbrough, Flintshire, Cannock)
    - cron: '12 8 * * *'  # statmap.co.uk/horizoNext nightly scrape (West Lindsey, East Staffordshire)
    - cron: '15 8 * * *'  # "Search/Advanced" family nightly scrape (Westmorland and Furness, Cherwell, Wychavon, Malvern Hills)
    - cron: '22 8 * * *'  # Barrow (Westmorland and Furness Council) nightly scrape — shares council_id with esl_scraper.py above, deliberately scheduled after it clears
    - cron: '28 8 * * *'  # OcellaWeb nightly scrape (Great Yarmouth, South Holland, Havering, Hillingdon)
    - cron: '32 8 * * *'  # Walsall (Swift/APAS) nightly scrape
    - cron: '35 8 * * *'  # Stratford-on-Avon (E-Planning v2.11) nightly scrape
    - cron: '40 8 * * *'  # Herefordshire nightly scrape
    - cron: '44 8 * * *'  # Medway (Open Digital Planning) nightly scrape
    - cron: '48 8 * * *'  # Ipswich (plain-ASP, httpx-only) nightly scrape
    - cron: '52 8 * * *'  # Central Bedfordshire (AcolNet) nightly scrape
    - cron: '54 8 * * *'  # West Dunbartonshire nightly scrape
    - cron: '55 8 * * *'  # Redcar and Cleveland nightly scrape
    - cron: '56 8 * * *'  # Kirklees nightly scrape
    - cron: '57 8 * * *'  # Amber Valley nightly scrape
    - cron: '58 8 * * *'  # South Derbyshire (Livewire) nightly scrape
    - cron: '59 8 * * *'  # Fylde (Planning + Building Control) nightly scrape
    - cron: '0 9 * * *'   # Ribble Valley nightly scrape
    - cron: '1 9 * * *'   # ODP Register nightly scrape (Barnet + Buckinghamshire)
    - cron: '2 9 * * *'   # alerts
    - cron: '0 8 * * 1'   # council health digest — weekly, Monday 8am
    # CHANGED 2026-08-18 — both moved to a shared every-5-days cadence
    # (real evidence: repeated frequent runs during development caused
    # real, observed issues on both — getApplications got a
    # vendor-wide bot-reputation flag after repeated testing, and the
    # Idox targeted group's whole reason for existing is that it's the
    # most WAF-sensitive part of the whole project). Deliberately
    # placed at 10am, well clear of the 9am alerts job and the whole
    # 3am-8:15am regular pipeline — scrape_idox_targeted alone can now
    # take up to ~90 minutes, which wouldn't safely fit in the old
    # tight pre-alerts slot. cron's day-of-month */5 isn't a perfectly
    # even 5-day gap across month boundaries, but that's fine here —
    # exact timing matters far less than just not hammering these
    # sensitive endpoints daily.
    - cron: '0 10 */5 * *'   # Idox targeted group (WAF-sensitive) — every ~5 days
    - cron: '20 10 */5 * *'  # getApplications family (Liverpool, Warrington, Newcastle, Blackburn & Darwen, Waltham Forest) — every ~5 days
  workflow_dispatch:
    inputs:
      job:
        description: 'Which job to run'
        required: true
        default: 'scrape'
        type: choice
        options: [scrape, scrape_bulk, scrape_idox_a, scrape_idox_b, scrape_idox_c, scrape_idox_d, scrape_idox_targeted, scrape_idox_bulk, scrape_arcus, scrape_arcus_bulk, scrape_civica, scrape_northgate, scrape_ni, alerts, council_health, arcus_recon, idox_month_test, idox_form_recon, idox_multi_recon, idox_recon_round2, civica_recon, civica_stalbans_results_recon, harrow_weekly_recon, northgate_recon, northgate_runnymede_results_recon, northgate_url_healthcheck, idox_gap_prober, idox_cheltenham_lewisham_verify, redbridge_recon, diagnose_runner_location, applicant_agent_recon, brightdata_unlocker_test, scraperapi_test, brightdata_browser_api_test, ni_planningsystemni_recon, getapplications_family_recon, scrape_getapplications, idox_waf_recheck, priority1_diagnostic, scraperapi_priority1_test, priority1_diagnostic_us_runner, derby_results_page_test, northgate_servlet_family_recon, northgate_servlet_scraper, northgate_south_tyneside_scraper, webshare_priority1_test, priority3_recon, statmap_weeklylist_recon, statmap_results_recon, statmap_weeklydate_recon, agileapplications_scraper, statmap_scraper, conwy_visibility_diagnostic, bromley_platform_check, wandf_recon, wandf_recon_round2, wandf_recon_round3, barrow_iframe_check, esl_pagination_test, esl_request_inspection, esl_next_link_test, esl_ajax_pagination_test, esl_real_click_test, esl_scraper, search_advanced_family_recon, nwarks_disclaimer_recon, nwarks_fullflow_test, barrow_scraper, barrow_decided_recon, esl_detail_page_recon, esl_cherwell_nwarks_detail_recon, test_nwarks_date_fetch_direct, nwarks_date_fetch_verbose_diag, ocellaweb_family_recon, ocellaweb_results_recon, ocellaweb_scraper, never_succeeded_diagnostic, oneoff_batch_recon, scambs_cambridge_url_check, gcambridge_separation_check, oneoff_round2_recon, oneoff_round3_recon, oneoff_round4_recon, walsall_scraper, stratford_search_flow_check, stratford_advanced_search_recon, stratford_real_submission_test, stratford_network_capture, stratford_scraper, herefordshire_real_results_recon, herefordshire_weekly_list_recon, herefordshire_wide_range_test, herefordshire_scraper, medway_search_recon, medway_scraper, charnwood_assure_recon, charnwood_search_recon, charnwood_guid_pagination_test, charnwood_scraper, ipswich_scraper, central_beds_scraper, backlog_batch_recon2, backlog_batch_recon3, west_dunbarton_scraper, redcar_cleveland_scraper, kirklees_scraper, telford_scraper, amber_valley_scraper, telford_diagnostic_recon, backlog_batch_recon4, south_derbyshire_scraper, fylde_scraper, ribblevalley_recon, ribblevalley_scraper, idox_candidate_verify, getapplications_isolation_diagnostic, webshare_idox_candidates_test, odp_register_scraper, barnet_page2_diagnostic, platform_family_verify, glasgow_edinburgh_arcgis_recon, glasgow_edinburgh_arcgis_recon2, agile_criteria_url_test]
      arcus_target_url:
        description: '(arcus_recon only) base URL to reconnoitre, e.g. https://example.my.site.com/pr/s — leave blank to default to Ashford as a sanity check'
        required: false
        default: ''
      arcus_target_name:
        description: '(arcus_recon only) human-readable name for the council being reconnoitred, purely for log readability'
        required: false
        default: ''

jobs:
  # ---------------------------------------------------------------------
  # diagnose_runner_location — added 2026-08-02, manual-trigger only.
  # Real test of a genuinely important hypothesis: GitHub's own
  # documentation confirms standard ubuntu-latest hosted runners run
  # from Azure's East US 2 region, not the UK — and several Idox
  # councils' WAFs may be geo-blocking or geo-throttling non-UK traffic
  # the same way a VPN showing as offshore gets blocked on these same
  # portals when tested manually. If confirmed, this would explain a
  # large fraction of the persistent 429/WAF blocks fought all session —
  # none of which responded to pacing, concurrency, or retry changes,
  # none of which touch WHERE the traffic appears to come from. Checks
  # two independent geolocation services for cross-confirmation, since
  # a single service could occasionally be wrong or stale.
  #
  # DELIBERATELY KEPT on ubuntu-latest (not moved to the UK runner) —
  # this is the whole point of the job, it needs to keep showing what
  # GitHub's OWN infrastructure presents as, as a permanent baseline to
  # compare the UK runner against.
  # ---------------------------------------------------------------------
  diagnose_runner_location:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'diagnose_runner_location'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check real IP and geolocation (ipinfo.io)
        run: |
          echo "=== ipinfo.io ==="
          curl -s https://ipinfo.io/json || echo "ipinfo.io request failed"
      - name: Check real IP and geolocation (ip-api.com)
        run: |
          echo "=== ip-api.com ==="
          curl -s http://ip-api.com/json || echo "ip-api.com request failed"
      - name: Summary
        run: |
          echo ""
          echo "If both services above report a country OTHER than GB/United"
          echo "Kingdom, that's real, direct confirmation our scraper traffic"
          echo "presents as non-UK to any council WAF checking geolocation —"
          echo "the same mechanism a VPN showing as offshore triggers on these"
          echo "same portals when tested manually."

  scrape:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape') ||
      (github.event_name == 'schedule' && github.event.schedule == '0 3 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Run harvester
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: cd scrapers && python data_gov_harvester.py

  scrape_bulk:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_bulk'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Run bulk harvester
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: cd scrapers && python data_gov_harvester.py --bulk

  # ---------------------------------------------------------------------
  # Idox nightly scrape — split into 4 batches. See idox_scraper.py's
  # --batch/--total-batches flags — the split is computed dynamically
  # from IDOX_COUNCILS' current length, nothing here needs updating as
  # councils are added or removed.
  # ---------------------------------------------------------------------
  scrape_idox_a:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_idox_a') ||
      (github.event_name == 'schedule' && github.event.schedule == '0 4 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 85
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Idox scraper — nightly batch 1 of 4 (last 14 days)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "75"
          DAYS_BACK: "14"
          CONCURRENCY: "2"
          REQUEST_DELAY_SECONDS: "0"
        run: cd scrapers && python idox_scraper.py --batch=1 --total-batches=4

  scrape_idox_b:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_idox_b') ||
      (github.event_name == 'schedule' && github.event.schedule == '50 4 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 85
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Idox scraper — nightly batch 2 of 4 (last 14 days)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "75"
          DAYS_BACK: "14"
          CONCURRENCY: "2"
          REQUEST_DELAY_SECONDS: "0"
        run: cd scrapers && python idox_scraper.py --batch=2 --total-batches=4

  scrape_idox_c:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_idox_c') ||
      (github.event_name == 'schedule' && github.event.schedule == '40 5 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 85
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Idox scraper — nightly batch 3 of 4 (last 14 days)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "75"
          DAYS_BACK: "14"
          CONCURRENCY: "2"
          REQUEST_DELAY_SECONDS: "0"
        run: cd scrapers && python idox_scraper.py --batch=3 --total-batches=4

  scrape_idox_d:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_idox_d') ||
      (github.event_name == 'schedule' && github.event.schedule == '30 6 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 85
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Idox scraper — nightly batch 4 of 4 (last 14 days)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "75"
          DAYS_BACK: "14"
          CONCURRENCY: "2"
          REQUEST_DELAY_SECONDS: "0"
        run: cd scrapers && python idox_scraper.py --batch=4 --total-batches=4

  scrape_idox_targeted:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_idox_targeted') ||
      (github.event_name == 'schedule' && github.event.schedule == '0 10 */5 * *')
    runs-on: [self-hosted, uk-runner]
    # CHANGED 2026-08-17 — real evidence: a full run against all 13
    # councils only reached 5 of them before the old 30-minute budget
    # ran out (Wakefield/Maidstone/Dover/Swale/Chichester all
    # succeeded, real data saved, real 429s handled correctly via
    # retry — genuinely not still-blocked, just ran out of time at
    # ~5-6 real minutes/council including backoff waits). 13 councils
    # at that observed pace needs ~65-70 minutes; budgeted generously
    # above that rather than risk the same shortfall again.
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Idox scraper — targeted group (confirmed WAF/429 councils)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "80"
          DAYS_BACK: "14"
          CONCURRENCY: "1"
          REQUEST_DELAY_SECONDS: "5"
        run: cd scrapers && python idox_scraper.py --targeted

  scrape_idox_bulk:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_idox_bulk'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 200
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Idox scraper — bulk (last 180 days, full council list)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "180"
          DAYS_BACK: "180"
          CONCURRENCY: "1"
        run: cd scrapers && python idox_scraper.py --bulk

  scrape_arcus:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_arcus') ||
      (github.event_name == 'schedule' && github.event.schedule == '20 7 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Arcus scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "30"
          CONCURRENCY: "2"
        run: cd scrapers && python arcus_scraper.py

  scrape_arcus_bulk:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_arcus_bulk'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Arcus scraper — bulk (180 days back for advanced_search councils)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "80"
          CONCURRENCY: "2"
          BULK_DAYS_BACK: "180"
        run: cd scrapers && python arcus_scraper.py --bulk

  scrape_civica:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_civica') ||
      (github.event_name == 'schedule' && github.event.schedule == '35 7 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright httpx
      - run: playwright install --with-deps chromium
      - name: Run Civica scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          CONCURRENCY: "2"
        run: cd scrapers && python civica_scraper.py

  idox_month_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_month_test'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Idox month-selection diagnostic
        run: cd scrapers && python idox_month_test.py

  idox_form_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_form_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Idox form-element recon
        run: cd scrapers && python idox_form_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: idox-form-recon-output
          path: /tmp/idox_form_recon.*
          retention-days: 7

  idox_multi_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_multi_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Idox multi-council recon (Renfrewshire/Brighton/Tonbridge/Brent)
        run: cd scrapers && python idox_multi_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: idox-multi-recon-output
          path: /tmp/idox_recon_*.html
          retention-days: 7

  idox_recon_round2:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_recon_round2'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Idox recon round 2 (real form-submit flow)
        run: cd scrapers && python idox_recon_round2.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: idox-recon-round2-output
          path: /tmp/idox_recon2_*.html
          retention-days: 7

  alerts:
    # CHANGED 2026-09-03 — shifted again from 1 9 to 2 9 to make room
    # for the ODP Register scraper (Barnet + Buckinghamshire) right
    # before it.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'alerts') ||
      (github.event_name == 'schedule' && github.event.schedule == '2 9 * * *')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Dispatch alerts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
          BASE_URL: ${{ secrets.BASE_URL }}
        run: python dispatch_alerts.py

  council_health:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'council_health') ||
      (github.event_name == 'schedule' && github.event.schedule == '0 8 * * 1')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Check council health
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
          ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}
        run: cd scrapers && python council_health_check.py

  arcus_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'arcus_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Arcus recon
        env:
          ARCUS_TARGET_URL: ${{ github.event.inputs.arcus_target_url }}
          ARCUS_TARGET_NAME: ${{ github.event.inputs.arcus_target_name }}
        run: cd scrapers && python arcus_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: arcus-recon-output
          path: /tmp/arcus_recon_*
          retention-days: 7

  civica_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'civica_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Civica recon (Wrexham, Northampton, Erewash)
        run: cd scrapers && python civica_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: civica-recon-output
          path: /tmp/civica_recon_*
          retention-days: 7

  civica_stalbans_results_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'civica_stalbans_results_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Civica Portal360 results-page recon (St Albans)
        run: cd scrapers && python civica_stalbans_results_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: civica-stalbans-results-recon-output
          path: /tmp/civica_stalbans_results.*
          retention-days: 7

  harrow_weekly_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'harrow_weekly_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Harrow weekly-list recon
        run: cd scrapers && python harrow_weekly_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: harrow-weekly-recon-output
          path: /tmp/harrow_weekly_recon.*
          retention-days: 7

  northgate_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'northgate_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Northgate recon (Birmingham, Islington, Runnymede, Tamworth)
        run: cd scrapers && python northgate_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: northgate-recon-output
          path: /tmp/northgate_recon_*
          retention-days: 7

  northgate_runnymede_results_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'northgate_runnymede_results_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Northgate results-page recon (Runnymede)
        run: cd scrapers && python northgate_runnymede_results_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: northgate-runnymede-results-recon-output
          path: /tmp/northgate_runnymede_results*
          retention-days: 7

  scrape_northgate:
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_northgate') ||
      (github.event_name == 'schedule' && github.event.schedule == '50 7 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright httpx beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Run Northgate scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          CONCURRENCY: "2"
        run: cd scrapers && python northgate_scraper.py

  scrape_ni:
    # PROMOTED to the nightly schedule (2026-08-17) after a confirmed
    # clean first production run — 290 real applications saved across
    # all 10 councils, no errors. Manual trigger kept available too,
    # same as every other platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_ni') ||
      (github.event_name == 'schedule' && github.event.schedule == '5 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      # NO Playwright install/cache here — CONFIRMED not needed. This
      # platform's real data comes from a direct REST API that accepts
      # plain httpx requests with no session/auth, unlike every other
      # scraper in this project. See ni_scraper.py's module docstring.
      - run: pip install httpx
      - name: Run NI Planning Portal scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "20"
          CONCURRENCY: "3"
        run: cd scrapers && python ni_scraper.py

  ni_planningsystemni_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'ni_planningsystemni_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run NI Planning Portal recon
        run: cd scrapers && python ni_planningsystemni_recon.py
      - name: Upload recon artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ni-recon-output
          path: /tmp/ni_recon_*
          retention-days: 14

  getapplications_family_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'getapplications_family_recon'
    # CHANGED 2026-08-17 — moved from ubuntu-latest to the self-hosted UK
    # runner after the first real Playwright run got an IDENTICAL block
    # (same "Error (IDX002)" title, same 7796-char body) as the earlier
    # plain httpx attempt. A real browser looking exactly like a non-
    # browser request to the WAF rules out a fingerprint/JS-detection
    # block — points instead at an IP/datacenter-range block, the exact
    # thing diagnose_runner_location already confirmed ubuntu-latest
    # presents as (Azure East US 2, not the UK). Testing the free,
    # already-built fix before reaching for anything paid.
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run getApplications-family recon (Liverpool, Warrington, Newcastle, Blackburn & Darwen)
        run: cd scrapers && python getapplications_family_recon.py
      - name: Upload recon artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: getapplications-family-recon-output
          path: /tmp/getapps_recon_*
          retention-days: 14

  scrape_getapplications:
    # PROMOTED to the nightly schedule (2026-08-17) after a confirmed
    # clean first production run — 194 real applications saved across
    # all 4 councils, no errors.
    #
    # DELIBERATELY on the UK runner from the START — that solves ONE of
    # two SEPARATE protective layers this platform has (an IP/
    # datacenter-range block). The OTHER layer, an AWS WAF JavaScript
    # challenge, needs a real browser regardless of which network the
    # request comes from — see getapplications_scraper.py's module
    # docstring for the full real-evidence trail (an earlier version of
    # this comment incorrectly said a browser wasn't needed at all;
    # corrected after two real production runs returned an AWS WAF
    # challenge page instead of real content).
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scrape_getapplications') ||
      (github.event_name == 'schedule' && github.event.schedule == '20 10 */5 * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run getApplications-family scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "20"
          # CHANGED 2026-08-17 — real evidence: at CONCURRENCY=2, the
          # first 2 councils (Liverpool, Warrington) succeeded cleanly,
          # but everything queued in right after started failing with
          # a NEW real signal (HTTP 405 "Human Verification") — a
          # different block than the AWS WAF JS challenge already
          # solved, and one Newcastle even passed on its FIRST request
          # before failing on later ones. That shape (fine, then
          # degrading) points at rate/behavioural detection tied to
          # request volume from this one shared runner IP, not a real
          # per-council block. Same category of problem the Idox
          # targeted group already solved by dropping to CONCURRENCY=1
          # — applying the same fix here rather than guessing at
          # something new.
          CONCURRENCY: "1"
          WEEKS_BACK: "2"
        run: cd scrapers && python getapplications_scraper.py

  idox_waf_recheck:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_waf_recheck'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recheck previously-confirmed-blocked Idox councils
        run: cd scrapers && python idox_waf_recheck.py
      - name: Upload recheck screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: idox-waf-recheck-screenshots
          path: /tmp/idox_waf_recheck_*
          retention-days: 14

  priority1_diagnostic:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'priority1_diagnostic'
    runs-on: [self-hosted, uk-runner]
    # REAL FIX (2026-08-25) — bumped from 15 to 25 minutes after a real
    # job cancellation: this diagnostic uses a genuine 120s timeout
    # PER council specifically to rule out "just slow" as an
    # explanation, and now tests 7 councils (North Kesteven just
    # added) — worst case that's up to 14 minutes of navigation
    # attempts alone, before even counting real setup overhead for
    # pip install and downloading Playwright's browser.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Diagnose Sheffield, Bassetlaw, North East Lincolnshire, Derby
        run: cd scrapers && python priority1_diagnostic.py
      - name: Upload diagnostic screenshots and HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: priority1-diagnostic-output
          path: /tmp/priority1_diag_*
          retention-days: 14

  priority1_diagnostic_us_runner:
    # DELIBERATELY temporary, one-off comparison job — NOT a permanent
    # addition to the regular rotation. Real question this answers: is
    # the Derby/NE Lincolnshire block specific to DigitalOcean, or does
    # ANY recognized cloud/datacenter IP get blocked, regardless of
    # provider or country? ubuntu-latest runs on Microsoft Azure (US),
    # a completely different cloud provider than the self-hosted
    # DigitalOcean UK runner — genuinely free to test (GitHub's own
    # standard hosted runner, no extra cost), and a different variable
    # than anything tested so far. NOTE: an earlier, unrelated finding
    # this session (the getApplications AWS WAF block) showed
    # ubuntu-latest getting blocked too — but that was CONFIRMED
    # country-based (US datacenter), a different mechanism than Derby/
    # NE Lincolnshire's block, which real evidence already ruled OUT as
    # country-based (a French VPN IP passed cleanly). Worth testing
    # fresh rather than assuming that earlier result predicts this one.
    # Same script as priority1_diagnostic above, completely unmodified
    # — only the runner differs, for a clean, direct comparison.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'priority1_diagnostic_us_runner'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Diagnose Sheffield, Bassetlaw, North East Lincolnshire, Derby (US/Azure runner)
        run: cd scrapers && python priority1_diagnostic.py
      - name: Upload diagnostic screenshots and HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: priority1-diagnostic-us-runner-output
          path: /tmp/priority1_diag_*
          retention-days: 14

  scraperapi_priority1_test:
    # ubuntu-latest is fine here (unlike every Playwright-based job in
    # this file) — the whole point of this test is that ScraperAPI's
    # OWN proxy IP is what reaches Derby/NE Lincolnshire, not whichever
    # runner sends the API call to ScraperAPI itself. No Playwright
    # needed either — this is a plain HTTP request to ScraperAPI's API.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scraperapi_priority1_test'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests beautifulsoup4
      - name: Test ScraperAPI standard tier against Derby + NE Lincolnshire
        env:
          SCRAPERAPI_KEY: ${{ secrets.SCRAPERAPI_KEY }}
        run: cd scrapers && python scraperapi_priority1_test.py
      - name: Upload saved HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraperapi-priority1-output
          path: /tmp/scraperapi_standard_*
          retention-days: 14

  derby_results_page_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'derby_results_page_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Test Derby results page directly
        run: cd scrapers && python derby_results_page_test.py
      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: derby-results-page-test-output
          path: /tmp/derby_results_test_*
          retention-days: 14

  northgate_servlet_family_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'northgate_servlet_family_recon'
    runs-on: [self-hosted, uk-runner]
    # CHANGED 2026-08-19 — real evidence: the first run got cut off
    # before Staffordshire Moorlands even started, likely because South
    # Tyneside's blocked-overlay click retried 58 times before giving
    # up, burning real time. Now fixed at the script level (real
    # overlay dismissal added), but keeping a generous timeout anyway
    # in case another council hits something similar.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon South Tyneside, Hartlepool, High Peak, Staffordshire Moorlands
        run: cd scrapers && python northgate_servlet_family_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: northgate-servlet-family-recon-output
          path: /tmp/northgate_servlet_recon_*
          retention-days: 14

  northgate_servlet_scraper:
    # PROMOTED to the nightly schedule (2026-08-19) after a confirmed
    # clean first production run — 60 real applications saved across
    # all 3 councils, no errors, 0.5 minutes total. Manual trigger kept
    # available too, same as every other platform's job, for ad-hoc
    # reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'northgate_servlet_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '55 7 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Northgate servlet-family scraper (Hartlepool, High Peak, Staffordshire Moorlands)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "20"
          CONCURRENCY: "1"
          DAYS_BACK: "30"
        run: cd scrapers && python northgate_servlet_scraper.py

  northgate_south_tyneside_scraper:
    # PROMOTED to the nightly schedule (2026-08-19) after a confirmed
    # clean first production run — 40 real applications saved across
    # all 4 pages, no errors, 0.2 minutes total. Manual trigger kept
    # available too, same as every other platform's job, for ad-hoc
    # reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'northgate_south_tyneside_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '0 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run South Tyneside scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "31"
        run: cd scrapers && python northgate_south_tyneside_scraper.py

  webshare_priority1_test:
    # ubuntu-latest is fine here (unlike every other Playwright-based
    # diagnostic in this file) — the whole point of a proxy is that IT
    # provides the real exit IP, not whichever machine initiates the
    # connection. Same reasoning as scraperapi_priority1_test.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'webshare_priority1_test'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Test Webshare residential proxy against Derby + NE Lincolnshire
        env:
          WEBSHARE_PROXY_HOST: ${{ secrets.WEBSHARE_PROXY_HOST }}
          WEBSHARE_PROXY_PORT: ${{ secrets.WEBSHARE_PROXY_PORT }}
          WEBSHARE_PROXY_USERNAME: ${{ secrets.WEBSHARE_PROXY_USERNAME }}
          WEBSHARE_PROXY_PASSWORD: ${{ secrets.WEBSHARE_PROXY_PASSWORD }}
        run: cd scrapers && python webshare_priority1_test.py
      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: webshare-priority1-test-output
          path: /tmp/webshare_proxy_test_*
          retention-days: 14

  webshare_idox_candidates_test:
    # ubuntu-latest is fine here — same reasoning as
    # webshare_priority1_test: the whole point of a proxy is that IT
    # provides the real exit IP. Tests whether a residential-ASN IP
    # gets past whatever's blocking 15 of the 20 councils from
    # idox_candidate_verify.py — real evidence there was a
    # deterministic (unchanged by inter-request pacing) per-target
    # block, most plausibly a datacenter-IP-range WAF rule.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'webshare_idox_candidates_test'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Test Webshare residential proxy against 5 parked Idox candidates
        env:
          WEBSHARE_PROXY_HOST: ${{ secrets.WEBSHARE_PROXY_HOST }}
          WEBSHARE_PROXY_PORT: ${{ secrets.WEBSHARE_PROXY_PORT }}
          WEBSHARE_PROXY_USERNAME: ${{ secrets.WEBSHARE_PROXY_USERNAME }}
          WEBSHARE_PROXY_PASSWORD: ${{ secrets.WEBSHARE_PROXY_PASSWORD }}
        run: cd scrapers && python webshare_idox_candidates_test.py
      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: webshare-idox-candidates-test-output
          path: /tmp/webshare_idox_test_*
          retention-days: 14

  odp_register_scraper:
    # PROMOTED to the nightly schedule (2026-09-03) after confirmed
    # clean technical execution across 3 real live runs — zero errors,
    # 0.1-0.4 minutes total. Two real bugs found and fixed along the
    # way: (1) Barnet's specific deployment has a genuine quirk where
    # page 1 is broken (confirmed via barnet_page2_diagnostic.py —
    # pages 2+ have real content, page 1 consistently doesn't) — fixed
    # by skipping page 1 rather than treating it as "reached the end";
    # (2) status was reading only the 'Status' field (workflow stage,
    # e.g. 'Determined') rather than the separate 'Council decision'
    # field (actual outcome, e.g. 'Granted') — fixed to use the richer
    # field. HONEST NOTE: 0 applications saved in testing so far is the
    # correct, expected outcome for a newly-onboarded pilot register
    # whose "recently published" feed is currently a batch of older
    # backlogged applications (same known-incomplete-pilot situation
    # already documented for Medway) — not a bug, should resolve
    # naturally as the backlog clears in future nightly runs. Manual
    # trigger kept available too, same as every other platform's job,
    # for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'odp_register_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '1 9 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run ODP Register scraper (Barnet + Buckinghamshire)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python odp_register_scraper.py

  barnet_page2_diagnostic:
    # Two live odp_register_scraper runs both found real pagination
    # (1, 2, ..., 4, Next page) but ZERO real <article>/<dl> content on
    # page 1 — even with an explicit 8s wait for a real <article>
    # element, ruling out simple timing. Checks pages 2 and 4 directly:
    # if THEY show real content while page 1 doesn't, that points to
    # something specific about page 1's default state.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'barnet_page2_diagnostic'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Check Barnet pages 2 and 4 for real content
        run: cd scrapers && python barnet_page2_diagnostic.py
      - name: Upload diagnostic HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: barnet-page2-diagnostic-output
          path: /tmp/barnet_page*_diagnostic.html
          retention-days: 14

  platform_family_verify:
    # Verifies 6 candidates claimed to be on platforms this project
    # already has working scrapers for (getapplications, Arcus,
    # agileapplications) — "same platform type" is NOT the same as
    # "confirmed working URL", same lesson already learned the hard
    # way with Fylde and Kirklees being wrongly assumed Idox earlier
    # this session.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'platform_family_verify'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Verify Harrow, Blaenau Gwent, Milton Keynes, Wiltshire, Pembrokeshire, Slough
        run: cd scrapers && python platform_family_verify.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: platform-family-verify-output
          path: /tmp/platform_verify_*
          retention-days: 14

  glasgow_edinburgh_arcgis_recon:
    # Real find from the user's own browsing: Glasgow publishes a
    # "Major and Significant Planning Applications Dashboard" via
    # ArcGIS Experience Builder, and a broader search surfaced OTHER
    # UK councils (Tonbridge & Malling, City of London, Fife)
    # publishing planning data via public ArcGIS FeatureServer REST
    # APIs — real, queryable JSON with structured fields, zero
    # scraping resistance. Probes Glasgow's dashboard config for its
    # underlying FeatureServer URL, and checks whether Edinburgh's
    # "applications received and decided map" is similarly ArcGIS-
    # backed.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'glasgow_edinburgh_arcgis_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright httpx
      - run: playwright install --with-deps chromium
      - name: Probe Glasgow's ArcGIS dashboard config and Edinburgh's map button
        run: cd scrapers && python glasgow_edinburgh_arcgis_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: glasgow-edinburgh-arcgis-recon-output
          path: /tmp/glasgow_experience_config.json
          retention-days: 14

  glasgow_edinburgh_arcgis_recon2:
    # Round 1 found zero literal FeatureServer URLs in Glasgow's
    # Experience Builder config (a known quirk — it often references
    # data sources by internal item ID rather than a literal URL) and
    # confirmed Edinburgh's map uses the older, simpler Web AppBuilder
    # framework instead (org-hosted at cityofedinburgh.maps.arcgis.com).
    # This resolves Glasgow's itemId references via follow-up lookups,
    # and fetches Edinburgh's Web AppBuilder config directly from its
    # own org endpoint.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'glasgow_edinburgh_arcgis_recon2'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Resolve Glasgow itemId refs and fetch Edinburgh's Web AppBuilder config
        run: cd scrapers && python glasgow_edinburgh_arcgis_recon2.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: glasgow-edinburgh-arcgis-recon2-output
          path: /tmp/edinburgh_webappviewer_config.json
          retention-days: 14

  agile_criteria_url_test:
    # Tests whether Pembrokeshire and Slough's agileapplications
    # instances support the same ?criteria={JSON}&page=1 URL shape the
    # existing 6 councils' scraper relies on, despite their visible UI
    # (form with radio buttons/date picker, a Terms & Conditions gate)
    # looking structurally different from the confirmed ng-table
    # pattern. Same domain/company, possibly a different UI generation
    # — same lesson already learned with Fylde/Kirklees this session.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'agile_criteria_url_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Test Pembrokeshire/Slough criteria-URL compatibility
        run: cd scrapers && python agile_criteria_url_test.py
      - name: Upload test HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agile-criteria-url-test-output
          path: /tmp/agile_criteria_test_*.html
          retention-days: 14

  priority3_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'priority3_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright httpx
      - run: playwright install --with-deps chromium
      - name: Recon agileapplications.co.uk and statmap.co.uk/horizoNext families
        run: cd scrapers && python priority3_recon.py
      - name: Upload recon HTML, screenshots, and raw HTTP output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: priority3-recon-output
          path: |
            /tmp/agile_recon_*
            /tmp/statmap_recon_*
          retention-days: 14

  statmap_weeklylist_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'statmap_weeklylist_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon statmap Weekly Lists tab
        run: cd scrapers && python statmap_weeklylist_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: statmap-weeklylist-recon-output
          path: /tmp/statmap_weeklylist_recon_*
          retention-days: 14

  statmap_results_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'statmap_results_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon statmap Weekly Lists real search results
        run: cd scrapers && python statmap_results_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: statmap-results-recon-output
          path: /tmp/statmap_results_recon_*
          retention-days: 14

  statmap_weeklydate_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'statmap_weeklydate_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon real weeklyListDate URL directly
        run: cd scrapers && python statmap_weeklydate_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: statmap-weeklydate-recon-output
          path: /tmp/statmap_weeklydate_recon_*
          retention-days: 14

  agileapplications_scraper:
    # PROMOTED to the nightly schedule (2026-08-21) after a confirmed
    # clean production run — 63 real applications saved across all 3
    # councils (20+25+18), correct and efficient (0.5 minutes, early-
    # exit pagination fix confirmed working). Manual trigger kept
    # available too, same as every other platform's job, for ad-hoc
    # reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'agileapplications_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '8 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run agileapplications.co.uk scraper (Middlesbrough, Flintshire, Cannock)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          CONCURRENCY: "1"
          DAYS_BACK: "14"
        run: cd scrapers && python agileapplications_scraper.py

  statmap_scraper:
    # PROMOTED to the nightly schedule (2026-08-21) after a confirmed
    # clean production run — 63 real applications saved across both
    # councils (42+21), both real timing issues (East Staffordshire's
    # slow-render race, the MUI pagination click) fixed and confirmed
    # working. Manual trigger kept available too, same as every other
    # platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'statmap_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '12 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run statmap.co.uk/horizoNext scraper (West Lindsey, East Staffordshire)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          CONCURRENCY: "1"
          WEEKS_BACK: "2"
        run: cd scrapers && python statmap_scraper.py

  conwy_visibility_diagnostic:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'conwy_visibility_diagnostic'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Diagnose Conwy's #rbRange visibility failure
        run: cd scrapers && python conwy_visibility_diagnostic.py
      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: conwy-visibility-diagnostic-output
          path: /tmp/conwy_diag_*
          retention-days: 14

  bromley_platform_check:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'bromley_platform_check'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Check which of Bromley's two platform URLs is actually live
        run: cd scrapers && python bromley_platform_check.py
      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bromley-platform-check-output
          path: /tmp/bromley_check_*
          retention-days: 14

  wandf_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'wandf_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Westmorland and Furness - Eden/South Lakeland register and Barrow Planning Hub
        run: cd scrapers && python wandf_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wandf-recon-output
          path: /tmp/wandf_recon_*
          retention-days: 14

  wandf_recon_round2:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'wandf_recon_round2'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Eden/South Lakeland Advanced Search and Barrow's real modal
        run: cd scrapers && python wandf_recon_round2.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wandf-recon-round2-output
          path: /tmp/wandf_r2_*
          retention-days: 14

  wandf_recon_round3:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'wandf_recon_round3'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Eden/South Lakeland real search + Barrow's real modal content
        run: cd scrapers && python wandf_recon_round3.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wandf-recon-round3-output
          path: /tmp/wandf_r3_*
          retention-days: 14

  barrow_iframe_check:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'barrow_iframe_check'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Check whether Barrow's modal content lives in a separate iframe
        run: cd scrapers && python barrow_iframe_check.py
      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: barrow-iframe-check-output
          path: |
            /tmp/barrow_iframe_check.png
            /tmp/barrow_frame_*.html
          retention-days: 14

  esl_pagination_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_pagination_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Test Eden/South Lakeland pagination via URL query params
        run: cd scrapers && python esl_pagination_test.py

  esl_request_inspection:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_request_inspection'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Inspect the real search request and hidden fields
        run: cd scrapers && python esl_request_inspection.py

  esl_next_link_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_next_link_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Confirm the real Next link returns genuinely different results
        run: cd scrapers && python esl_next_link_test.py

  esl_ajax_pagination_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_ajax_pagination_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Confirm the real ResultsPage AJAX endpoint works directly
        run: cd scrapers && python esl_ajax_pagination_test.py

  esl_real_click_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_real_click_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Actually click the real Next element and see what happens
        run: cd scrapers && python esl_real_click_test.py
      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: esl-real-click-test-output
          path: /tmp/esl_next_click_result.png
          retention-days: 14

  esl_scraper:
    # PROMOTED to the nightly schedule (2026-08-22), extended to the
    # full "Search/Advanced" platform family (2026-08-23) after a
    # confirmed clean production run across all 4 councils — 461 real
    # applications saved (Westmorland and Furness 110, Cherwell 113,
    # Wychavon 161, Malvern Hills 77), all matching their real
    # confirmed totals exactly, 5.7 minutes total (comfortably inside
    # the 15-minute budget). Manual trigger kept available too, same
    # as every other platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '15 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Eden/South Lakeland scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python esl_scraper.py

  search_advanced_family_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'search_advanced_family_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Cherwell, North Warwickshire, Wychavon, Malvern Hills
        run: cd scrapers && python search_advanced_family_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: search-advanced-family-recon-output
          path: /tmp/search_advanced_recon_*
          retention-days: 14

  nwarks_disclaimer_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'nwarks_disclaimer_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon North Warwickshire's disclaimer gate
        run: cd scrapers && python nwarks_disclaimer_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nwarks-disclaimer-recon-output
          path: /tmp/nwarks_disclaimer_recon.*
          retention-days: 14

  nwarks_fullflow_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'nwarks_fullflow_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Test North Warwickshire's full flow after accepting disclaimer
        run: cd scrapers && python nwarks_fullflow_test.py
      - name: Upload results HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nwarks-fullflow-test-output
          path: /tmp/nwarks_fullflow_results.*
          retention-days: 14

  barrow_scraper:
    # PROMOTED to the nightly schedule (2026-08-24) after a confirmed
    # clean production run — 45 real records saved (18 new
    # applications, 27 real decision status updates matched by
    # reference number), all 4 weeks handled correctly across both
    # Validated and Decided lists, 0.4 minutes total. The stuck-modal
    # cascading-failure bug from the first live attempt is confirmed
    # fixed. Manual trigger kept available too, same as every other
    # platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'barrow_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '22 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Barrow scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python barrow_scraper.py

  barrow_decided_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'barrow_decided_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Barrow's real Decided list structure
        run: cd scrapers && python barrow_decided_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: barrow-decided-recon-output
          path: |
            /tmp/barrow_decided_frame.html
            /tmp/barrow_decided_recon.png
          retention-days: 14

  esl_detail_page_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_detail_page_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Eden/South Lakeland's real detail page for a date field
        run: cd scrapers && python esl_detail_page_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: esl-detail-page-recon-output
          path: |
            /tmp/esl_detail_page.html
            /tmp/esl_detail_page.png
          retention-days: 14

  esl_cherwell_nwarks_detail_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'esl_cherwell_nwarks_detail_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Cherwell and North Warwickshire's real detail pages
        run: cd scrapers && python esl_cherwell_nwarks_detail_recon.py

  test_nwarks_date_fetch_direct:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'test_nwarks_date_fetch_direct'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Directly test the real fetch_received_dates fix for North Warwickshire
        run: cd scrapers && python test_nwarks_date_fetch_direct.py

  nwarks_date_fetch_verbose_diag:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'nwarks_date_fetch_verbose_diag'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Verbose diagnostic of North Warwickshire's real date fetch
        run: cd scrapers && python nwarks_date_fetch_verbose_diag.py
      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nwarks-date-fetch-verbose-diag-output
          path: /tmp/nwarks_verbose_diag.png
          retention-days: 14

  ocellaweb_family_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'ocellaweb_family_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Great Yarmouth, South Holland, Havering, Hillingdon
        run: cd scrapers && python ocellaweb_family_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ocellaweb-family-recon-output
          path: /tmp/ocellaweb_recon_*
          retention-days: 14

  ocellaweb_results_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'ocellaweb_results_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Submit a real OcellaWeb search and capture results structure
        run: cd scrapers && python ocellaweb_results_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ocellaweb-results-recon-output
          path: |
            /tmp/ocellaweb_results_recon.html
            /tmp/ocellaweb_results_recon.png
          retention-days: 14

  ocellaweb_scraper:
    # PROMOTED to the nightly schedule (2026-08-25) after a confirmed
    # clean production run — 67 real applications saved across all 4
    # councils (Great Yarmouth 26, South Holland 10, Havering 6,
    # Hillingdon 25), zero errors, zero per-council quirks needed
    # despite only Great Yarmouth's results page ever being directly
    # search-tested beforehand, 0.3 minutes total. Havering and
    # Hillingdon both close a genuine gap from an earlier London
    # coverage audit. Manual trigger kept available too, same as every
    # other platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'ocellaweb_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '28 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run OcellaWeb scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python ocellaweb_scraper.py

  never_succeeded_diagnostic:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'never_succeeded_diagnostic'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Diagnose East Riding, Stafford, North Kesteven
        run: cd scrapers && python never_succeeded_diagnostic.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: never-succeeded-diagnostic-output
          path: /tmp/never_succeeded_diag_*
          retention-days: 14

  oneoff_batch_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'oneoff_batch_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Charnwood, Walsall, Central Beds, Ipswich, Stratford, Herefordshire, Greater Cambridge, Medway
        run: cd scrapers && python oneoff_batch_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oneoff-batch-recon-output
          path: /tmp/oneoff_recon_*
          retention-days: 14

  scambs_cambridge_url_check:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scambs_cambridge_url_check'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Check whether South Cambridgeshire/Cambridge City's existing URLs still work
        run: cd scrapers && python scambs_cambridge_url_check.py

  gcambridge_separation_check:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'gcambridge_separation_check'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Check whether the shared portal can filter by specific council
        run: cd scrapers && python gcambridge_separation_check.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gcambridge-separation-check-output
          path: |
            /tmp/gcambridge_separation_check.html
            /tmp/gcambridge_separation_check.png
          retention-days: 14

  oneoff_round2_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'oneoff_round2_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Submit real searches on Stratford, Herefordshire, Walsall
        run: cd scrapers && python oneoff_round2_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oneoff-round2-recon-output
          path: /tmp/oneoff_r2_*
          retention-days: 14

  oneoff_round3_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'oneoff_round3_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Ipswich advanced search + Central Beds more-search-options
        run: cd scrapers && python oneoff_round3_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oneoff-round3-recon-output
          path: /tmp/oneoff_r3_*
          retention-days: 14

  oneoff_round4_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'oneoff_round4_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Submit real date-range searches on Ipswich and Central Beds
        run: cd scrapers && python oneoff_round4_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oneoff-round4-recon-output
          path: /tmp/oneoff_r4_*
          retention-days: 14

  walsall_scraper:
    # PROMOTED to the nightly schedule (2026-08-27) after a confirmed
    # clean production run — 58 real applications saved, all 6 pages
    # matching the real confirmed total exactly, 0.3 minutes total.
    # Manual trigger kept available too, same as every other platform's
    # job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'walsall_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '32 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Walsall scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python walsall_scraper.py

  stratford_search_flow_check:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'stratford_search_flow_check'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Check Stratford's real Search button flow
        run: cd scrapers && python stratford_search_flow_check.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stratford-search-flow-check-output
          path: |
            /tmp/stratford_search_page.html
            /tmp/stratford_search_page.png
          retention-days: 14

  stratford_advanced_search_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'stratford_advanced_search_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Stratford's real Advanced Search page
        run: cd scrapers && python stratford_advanced_search_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stratford-advanced-search-recon-output
          path: |
            /tmp/stratford_advanced_search.html
            /tmp/stratford_advanced_search.png
          retention-days: 14

  stratford_real_submission_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'stratford_real_submission_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Submit a real Stratford Advanced Search and capture results
        run: cd scrapers && python stratford_real_submission_test.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stratford-real-submission-test-output
          path: |
            /tmp/stratford_real_results.html
            /tmp/stratford_real_results.png
          retention-days: 14

  stratford_network_capture:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'stratford_network_capture'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Capture the real network request behind Stratford's search
        run: cd scrapers && python stratford_network_capture.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stratford-network-capture-output
          path: |
            /tmp/stratford_correct_results.html
            /tmp/stratford_correct_results.png
          retention-days: 14

  stratford_scraper:
    # PROMOTED to the nightly schedule (2026-08-27) after a confirmed
    # clean production run — 139 real applications saved, all 6 pages
    # matching the real confirmed total exactly, 0.3 minutes total.
    # Genuinely the most stubborn platform in this batch to get
    # working (6 real diagnostic rounds), now fully resolved. Manual
    # trigger kept available too, same as every other platform's job,
    # for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'stratford_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '35 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Stratford scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python stratford_scraper.py

  herefordshire_real_results_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'herefordshire_real_results_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Herefordshire's real full results page
        run: cd scrapers && python herefordshire_real_results_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: herefordshire-real-results-recon-output
          path: |
            /tmp/herefordshire_real_results.html
            /tmp/herefordshire_real_results.png
          retention-days: 14

  herefordshire_weekly_list_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'herefordshire_weekly_list_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Herefordshire's real Weekly List tab
        run: cd scrapers && python herefordshire_weekly_list_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: herefordshire-weekly-list-recon-output
          path: |
            /tmp/herefordshire_weekly_list.html
            /tmp/herefordshire_weekly_list.png
          retention-days: 14

  herefordshire_wide_range_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'herefordshire_wide_range_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Test a direct 30-day range URL for Herefordshire
        run: cd scrapers && python herefordshire_wide_range_test.py

  herefordshire_scraper:
    # PROMOTED to the nightly schedule (2026-08-28) after a confirmed
    # clean production run — 377 real applications saved, all 38
    # pages matching the real confirmed total exactly, 4.0 minutes
    # total (genuinely one of the longer runs, given the confirmed
    # real volume — MAX_PAGES bumped from 30 to 60 after the first
    # attempt correctly stopped early at the original cap). Manual
    # trigger kept available too, same as every other platform's job,
    # for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'herefordshire_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '40 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Herefordshire scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
          MAX_PAGES: "60"
        run: cd scrapers && python herefordshire_scraper.py

  medway_search_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'medway_search_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Recon Medway's real search behavior and pagination
        run: cd scrapers && python medway_search_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: medway-search-recon-output
          path: |
            /tmp/medway_search_recon.html
            /tmp/medway_search_recon.png
          retention-days: 14

  medway_scraper:
    # PROMOTED to the nightly schedule (2026-08-28) after a confirmed
    # clean production run — 18 real applications across the whole
    # register (genuinely confirmed complete via the real 'no more
    # Next page' signal), 2 within the 30-day window saved, recheck
    # correctly ran against 10 previously-pending applications, 0.4
    # minutes total. Real, honest note: this is a known-incomplete
    # Open Digital Planning pilot register per the council's own text
    # — small numbers here reflect the real, limited scope of the
    # source, not a scraper problem. Manual trigger kept available
    # too, same as every other platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'medway_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '44 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Medway scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python medway_scraper.py

  charnwood_assure_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'charnwood_assure_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Charnwood's real Assure platform
        run: cd scrapers && python charnwood_assure_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: charnwood-assure-recon-output
          path: |
            /tmp/charnwood_assure_recon.html
            /tmp/charnwood_assure_recon.png
          retention-days: 14

  charnwood_search_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'charnwood_search_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Submit a real Charnwood search and capture results
        run: cd scrapers && python charnwood_search_recon.py
      - name: Upload recon HTML and screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: charnwood-search-recon-output
          path: |
            /tmp/charnwood_search_results.html
            /tmp/charnwood_search_results.png
          retention-days: 14

  charnwood_guid_pagination_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'charnwood_guid_pagination_test'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4
      - run: playwright install --with-deps chromium
      - name: Test Charnwood GUID stability and pagination
        run: cd scrapers && python charnwood_guid_pagination_test.py
      - name: Upload page 2 HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: charnwood-guid-pagination-test-output
          path: |
            /tmp/charnwood_page2.html
            /tmp/charnwood_no_pagination_debug.html
          retention-days: 14

  charnwood_scraper:
    # DELIBERATELY manual-trigger only for now — brand new platform,
    # never run live, matching every other new platform's first job
    # this session before a confirmed clean production run. Genuinely
    # the most stubborn platform this session — worth watching this
    # first run especially closely.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'charnwood_scraper'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Charnwood scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "20"
          DAYS_BACK: "30"
        run: cd scrapers && python charnwood_scraper.py

  ipswich_scraper:
    # PROMOTED to the nightly schedule (2026-08-30) after a confirmed
    # clean first production run — 80 real applications saved across
    # 8 pages, zero errors, 0.4 minutes total. Genuinely the simplest
    # platform in the project — NO Playwright install/cache here —
    # CONFIRMED not needed. This platform's real data comes from plain
    # GET requests with no session/CSRF/cookie needed anywhere, same
    # category as ni_scraper.py. See ipswich_councils.py's module
    # docstring for the full real-evidence trail. Manual trigger kept
    # available too, same as every other platform's job, for ad-hoc
    # reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'ipswich_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '48 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx beautifulsoup4
      - name: Run Ipswich scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python ipswich_scraper.py

  central_beds_scraper:
    # PROMOTED to the nightly schedule (2026-08-30) after a confirmed
    # clean first production run — 177 real applications saved across
    # 18 pages, zero errors, 0.7 minutes total. Real confirmed volume
    # is meaningfully higher than most platforms in this project (402
    # real applications across a ~2 month window during recon; 177 for
    # the real 30-day production window) — timeout budgeted generously
    # given up to 60 pages of session-bound click-through pagination.
    # See central_beds_councils.py's module docstring for the full
    # real-evidence trail. Manual trigger kept available too, same as
    # every other platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'central_beds_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '52 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Central Bedfordshire scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "20"
          DAYS_BACK: "30"
        run: cd scrapers && python central_beds_scraper.py

  backlog_batch_recon2:
    # First-pass recon on 11 of the "zero-recon backlog" councils,
    # using the user's own manually-verified URLs (more reliable than
    # web search — an earlier version of this job used web-search-
    # derived URLs, some of which were wrong, e.g. Fylde). Gedling
    # confirmed genuinely on Idox — not recon'd here, goes straight
    # into idox_councils.py instead.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'backlog_batch_recon2'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon West Dunbartonshire, Redcar-Cleveland, Ribble Valley, Fylde, Kirklees, Rotherham, North Lincolnshire, Telford, South Derbyshire, Amber Valley, Boston
        run: cd scrapers && python backlog_batch_recon2.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backlog-batch-recon2-output
          path: /tmp/backlog2_recon_*
          retention-days: 14

  backlog_batch_recon3:
    # Follow-up recon resolving 4 remaining ambiguities from
    # backlog_batch_recon2: Amber Valley's real JSON web-service API
    # (plain httpx, no Playwright needed for this part), Fylde's
    # disclaimer gate, Rotherham's weekly-list submission, and whether
    # South Derbyshire's Livewire component genuinely accepts URL
    # query-string date filters.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'backlog_batch_recon3'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright httpx
      - run: playwright install --with-deps chromium
      - name: Recon Amber Valley API, Fylde disclaimer flow, Rotherham weekly list, South Derbyshire URL params
        run: cd scrapers && python backlog_batch_recon3.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backlog-batch-recon3-output
          path: /tmp/backlog3_recon_*
          retention-days: 14

  northgate_url_healthcheck:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'northgate_url_healthcheck'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Run Northgate stored-URL health check
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: cd scrapers && python northgate_url_healthcheck.py

  idox_gap_prober:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_gap_prober'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Run Idox gap-list batch prober
        run: cd scrapers && python idox_gap_prober.py

  idox_cheltenham_lewisham_verify:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_cheltenham_lewisham_verify'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Cheltenham/Lewisham redirect verification
        run: cd scrapers && python idox_cheltenham_lewisham_verify.py

  redbridge_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'redbridge_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run Redbridge recon
        run: cd scrapers && python redbridge_recon.py
      - name: Upload recon output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: redbridge-recon-output
          path: /tmp/redbridge_recon.*
          retention-days: 7

  # ---------------------------------------------------------------------
  # Applicant/agent data recon — manual trigger only, no schedule.
  #
  # Built 2026-08-13, before committing any code to exposing applicant/
  # agent intelligence (flagged in an external product assessment as
  # the single biggest commercial opportunity). Real, unverified
  # concern before committing: does a real Idox LIST view expose
  # applicant/agent name at all, or does it only ever live on each
  # individual application's own detail page? If the latter, exposing
  # this at scale means visiting a separate page per application rather
  # than one page per ~10 (the list view) — directly compounding the
  # WAF/429 blocking problem this whole session already fought hard to
  # understand. Prints real, direct findings straight to the log —
  # purely diagnostic, doesn't touch Supabase at all.
  # ---------------------------------------------------------------------
  applicant_agent_recon:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'applicant_agent_recon'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Run applicant/agent data recon
        run: cd scrapers && python applicant_agent_recon.py

  # ---------------------------------------------------------------------
  # Bright Data Web Unlocker API test — manual trigger only, no schedule.
  #
  # Built 2026-08-16, testing whether Web Unlocker API can get past
  # whatever's blocking Aberdeenshire Council (confirmed real "Page load
  # timeout" via idox_multi_recon.py — same signature as the existing
  # ~13-council WAF-blocked group). Plain HTTP requests only, no
  # Playwright/browser needed — Web Unlocker's own docs are explicit
  # it's built for single, stateless requests, not browser sessions.
  # Credentials read from GitHub Secrets, never hardcoded here or in the
  # script itself.
  # ---------------------------------------------------------------------
  brightdata_unlocker_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'brightdata_unlocker_test'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - name: Run Bright Data Web Unlocker API test
        env:
          BRIGHTDATA_API_KEY: ${{ secrets.BRIGHTDATA_API_KEY }}
          BRIGHTDATA_ZONE_NAME: ${{ secrets.BRIGHTDATA_ZONE_NAME }}
        run: cd scrapers && python test_brightdata_unlocker.py

  # ---------------------------------------------------------------------
  # ScraperAPI test — manual trigger only, no schedule.
  #
  # Built 2026-08-16. All three residential proxy providers checked so
  # far (Bright Data, Oxylabs, Decodo) explicitly restrict government
  # sites behind KYC/ID verification. Worth testing ScraperAPI directly
  # rather than trust a secondhand, unverified claim that it doesn't.
  # Same three confirmed-blocked councils as the Bright Data test, for a
  # fair, direct comparison. Credentials read from GitHub Secrets, never
  # hardcoded here or in the script itself.
  # ---------------------------------------------------------------------
  scraperapi_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'scraperapi_test'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests beautifulsoup4
      - name: Run ScraperAPI test
        env:
          SCRAPERAPI_KEY: ${{ secrets.SCRAPERAPI_KEY }}
        run: cd scrapers && python test_scraperapi.py

  # ---------------------------------------------------------------------
  # Bright Data Browser API test — manual trigger only, no schedule.
  #
  # Built 2026-08-16, after seven rounds of testing ScraperAPI's plain
  # HTTP approach against a CSRF-protected Idox form all landed on the
  # same consistent 403. Browser API is a real remote browser connected
  # to via Playwright's connect_over_cdp() — our own already-proven
  # form-interaction logic can run essentially unchanged, sidestepping
  # the whole CSRF/cookie puzzle entirely. Only playwright itself is
  # needed here, NOT a local Chromium install — the actual browser runs
  # on Bright Data's own infrastructure, not the runner.
  # ---------------------------------------------------------------------
  brightdata_browser_api_test:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'brightdata_browser_api_test'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install playwright
      - name: Run Bright Data Browser API test
        env:
          BRIGHTDATA_BROWSER_AUTH: ${{ secrets.BRIGHTDATA_BROWSER_AUTH }}
        run: cd scrapers && python test_brightdata_browser_api.py

  west_dunbarton_scraper:
    # PROMOTED to the nightly schedule (2026-08-31) after a confirmed
    # clean production run — 5 real applications saved, zero errors,
    # 0.1 minutes total. Real fix needed along the way: OpenSSL 3.x's
    # default cipher security level (SECLEVEL=2) rejected this old
    # council server's weak ciphers outright, regardless of TLS
    # version — fixed via SECLEVEL=0 in the custom SSL context (see
    # west_dunbarton_scraper.py's _legacy_ssl_context()). Manual
    # trigger kept available too, same as every other platform's job,
    # for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'west_dunbarton_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '54 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx beautifulsoup4
      - name: Run West Dunbartonshire scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python west_dunbarton_scraper.py

  redcar_cleveland_scraper:
    # PROMOTED to the nightly schedule (2026-08-31) after a confirmed
    # clean production run — 2 real applications saved, zero errors,
    # 0.1 minutes total. Real fix needed along the way: the generic
    # result-row detection could match the same reference more than
    # once (a real Postgres "ON CONFLICT DO UPDATE...affect row a
    # second time" error on the first attempt) — fixed with a
    # dedup-by-reference pass. Manual trigger kept available too, same
    # as every other platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'redcar_cleveland_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '55 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Redcar and Cleveland scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python redcar_cleveland_scraper.py

  kirklees_scraper:
    # PROMOTED to the nightly schedule (2026-08-31) after a confirmed
    # clean production run — 3 real applications saved, zero errors,
    # 0.1 minutes total, genuinely worked cleanly on the very first
    # live attempt. Corrects an existing-but-wrong Idox DB row (see
    # kirklees_councils.py's module docstring). Manual trigger kept
    # available too, same as every other platform's job, for ad-hoc
    # reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'kirklees_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '56 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Kirklees scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python kirklees_scraper.py

  telford_scraper:
    # DELIBERATELY manual-trigger only for now — brand new platform,
    # never run live. Search FORM confirmed real; RESULTS structure is
    # UNCONFIRMED (never actually submitted during recon) — built with
    # generic/defensive result-row detection and heavy diagnostic
    # logging. See telford_councils.py's module docstring.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'telford_scraper'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Telford and Wrekin scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python telford_scraper.py

  amber_valley_scraper:
    # PROMOTED to the nightly schedule (2026-08-31) after a confirmed
    # clean production run — 416 real applications saved, zero errors,
    # under a minute total. Genuinely the best platform in the whole
    # project — a real, documented JSON web-service API, no Playwright
    # or HTML parsing needed anywhere. HONEST LIMITATION: the feed's
    # real 'decision'/'status' fields are null even for decided
    # applications — see amber_valley_councils.py's module docstring.
    # Manual trigger kept available too, same as every other
    # platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'amber_valley_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '57 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx
      - name: Run Amber Valley scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python amber_valley_scraper.py

  telford_diagnostic_recon:
    # Two live telford_scraper runs both hit a customErrors redirect
    # (?aspxerrorpath=...) with no visible cause. This attaches a
    # response listener during submission and tries alternate date
    # formats to find the real trigger before guessing at another fix.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'telford_diagnostic_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Diagnose Telford's aspxerrorpath redirect
        run: cd scrapers && python telford_diagnostic_recon.py

  backlog_batch_recon4:
    # Real UI interaction (not shortcuts) for the 3 remaining backlog
    # items: South Derbyshire's Livewire dateType selection (the real
    # afterDate/beforeDate inputs are disabled="" until a genuine
    # dateType selection fires their reactive enable), Rotherham's
    # sort-order selection (previously blank, likely why blind submits
    # returned nothing), and Fylde's real search submission (shares
    # near-identical field names to Redcar & Cleveland, already live).
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'backlog_batch_recon4'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon South Derbyshire Livewire interaction, Rotherham sort submit, Fylde real search
        run: cd scrapers && python backlog_batch_recon4.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backlog-batch-recon4-output
          path: /tmp/backlog4_recon_*
          retention-days: 14

  south_derbyshire_scraper:
    # PROMOTED to the nightly schedule (2026-08-31) after a confirmed
    # clean production run — 83 real applications saved, zero errors,
    # 0.2 minutes total. Real Livewire UI interaction (select dateType,
    # fill now-enabled date fields) genuinely filters the dataset —
    # confirmed real total dropped from 32,240 unfiltered down to a
    # real, correct 83 for the requested window. Real bug caught and
    # fixed before this ever ran live: two wire:effects elements exist
    # on the page (one empty, one real) — querySelector() alone grabbed
    # the wrong one; fixed to search all of them for the one with real
    # dispatch data. HONEST LIMITATION: pagination not yet implemented
    # (perPage capped at 100) — see south_derbyshire_scraper.py's
    # module docstring. Manual trigger kept available too, same as
    # every other platform's job, for ad-hoc reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'south_derbyshire_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '58 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright httpx
      - run: playwright install --with-deps chromium
      - name: Run South Derbyshire scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python south_derbyshire_scraper.py

  fylde_scraper:
    # PROMOTED to the nightly schedule (2026-08-31) after a confirmed
    # clean production run — 327 real applications saved (50 Planning
    # + 277 Building Control), zero errors, 0.8 minutes total. Two real
    # bugs caught and fixed along the way: (1) navigating directly to
    # the AJAX-backed /Search/ResultsPage/N?module=... URLs silently
    # returned nothing beyond page 1 — fixed by clicking the real
    # "Next" link instead; (2) even with real clicks, a race condition
    # between the AJAX swap completing and page.content() being read
    # caused premature "0 new" stops (confirmed via a spot real-count
    # check from the user's own browser: 638 real Building Control
    # apps in a 61-day window vs only 20 captured) — fixed with a
    # longer settle time plus an explicit stale-content detection/
    # retry. MAX_PAGES also raised from 20 to 50 for Building Control's
    # real volume. Includes Building Control (dormer extensions,
    # extensions, etc.) alongside Planning per explicit request — see
    # fylde_scraper.py's module docstring. Manual trigger kept
    # available too, same as every other platform's job, for ad-hoc
    # reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'fylde_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '59 8 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run Fylde scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python fylde_scraper.py

  ribblevalley_recon:
    # New intel (2026-09-01) from the user's own browsing: a real HTML
    # advanced search exists (webportal.ribblevalley.gov.uk/
    # planningApplication/search/advanced), not just PDF weekly lists
    # as the original manual recon found. Submits a real Decision Date
    # Between search and follows one detail link to confirm the real
    # field names and per-application page structure before building
    # anything.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'ribblevalley_recon'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Recon Ribble Valley advanced search, results, and detail page
        run: cd scrapers && python ribblevalley_recon.py
      - name: Upload recon HTML and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ribblevalley-recon-output
          path: /tmp/ribblevalley_recon_*
          retention-days: 14

  ribblevalley_scraper:
    # PROMOTED to the nightly schedule (2026-09-01) after a confirmed
    # clean production run — 66 real applications saved across 7 pages
    # (pagination confirmed exact, matching the real total precisely),
    # zero errors, 0.7 minutes total. Genuinely the simplest tier in
    # the project alongside Ipswich/West Dunbartonshire/NI — plain GET
    # requests throughout, no Playwright/session/CSRF needed anywhere.
    # Real status vocabulary discovered and added: "PERMISSION NOT
    # REQUIRED" is a genuine positive decided outcome, mapped to
    # 'approved' (similar real-world case to Ipswich's "No Objection").
    # HONEST LIMITATION: the only date-range search is by DECISION
    # date, not received/submitted date — recently-submitted-but-
    # undecided applications may be under-covered. See
    # ribblevalley_councils.py's module docstring. Manual trigger kept
    # available too, same as every other platform's job, for ad-hoc
    # reruns.
    if: >
      (github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'ribblevalley_scraper') ||
      (github.event_name == 'schedule' && github.event.schedule == '0 9 * * *')
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install httpx beautifulsoup4
      - name: Run Ribble Valley scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          MAX_MINUTES: "15"
          DAYS_BACK: "30"
        run: cd scrapers && python ribblevalley_scraper.py

  idox_candidate_verify:
    # Verifies 19 candidate councils from the user's newest manual
    # recon list, all labelled "(IDOX)" — but a URL LOOKING like Idox
    # isn't confirmation (Fylde taught us this the hard way). Checks
    # each for real Idox markers, blocks, or redirects before any of
    # these get added to idox_councils.py's live list.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'idox_candidate_verify'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright
      - run: playwright install --with-deps chromium
      - name: Verify 19 candidate Idox councils
        run: cd scrapers && python idox_candidate_verify.py

  getapplications_isolation_diagnostic:
    # Diagnoses a real regression: the full 12-council getapplications
    # run saved Liverpool cleanly then hit HTTP 405 "Human
    # Verification" on every subsequent council — including 3
    # previously-confirmed-working ones (Warrington, Newcastle,
    # Blackburn with Darwen). Tests whether this is a genuine platform
    # change vs a cross-domain volume trigger (visiting many different
    # council instances of the same shared platform in one session).
    # See getapplications_isolation_diagnostic.py's module docstring.
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.job == 'getapplications_isolation_diagnostic'
    runs-on: [self-hosted, uk-runner]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1
      - run: pip install playwright beautifulsoup4 httpx
      - run: playwright install --with-deps chromium
      - name: Run isolation diagnostic (Liverpool+Warrington, then Warrington alone)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: cd scrapers && python getapplications_isolation_diagnostic.py
